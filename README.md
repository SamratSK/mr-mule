# Mr Mule - Live Testing Interface

This app is a lightweight WebSocket broadcaster for live transaction testing. It serves a simple endpoint that streams CSV-formatted transaction rows at a steady cadence so MuleGuard AI can connect, ingest real-time data, and validate the live inference + graphing pipeline end-to-end.
